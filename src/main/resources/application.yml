spring:
  datasource:
    drive-class-name: postgres
    url: jdbc:postgresql://localhost:5432/study-db
    username: postgres
    password: ${DATABASE_PASSWORD:123455}
  sql:
    init:
      platform: postgres